// Code generated by go-sdk-windowscsp-codegen. DO NOT EDIT.

package settings_user

import (
	"context"

	"github.com/deploymenttheory/go-sdk-windowscsp/windowscsp/client"
)

// GetConfigureTaskbarCalendar reads ./User/Vendor/MSFT/Policy/Config/Settings/ConfigureTaskbarCalendar.
// Allows IT Admins to configure the default setting for showing additional calendars (besides the
// default calendar for the locale) in the taskbar clock and calendar flyout. In this version of
// Windows 10, supported additional calendars are: Simplified or Traditional Chinese lunar
// calendar. Turning on one of these calendars will display Chinese lunar dates below the default
// calendar for the locale. Select Don't show additional calendars to prevent showing other
// calendars besides the default calendar for the locale.
//
// Default: 0.
// Supported from OS build 10.0.15063 (CSP v5.0).
func (s *SettingsUser) GetConfigureTaskbarCalendar(ctx context.Context) (int64, error) {
	v, err := s.c.Get(ctx, URIConfigureTaskbarCalendar)
	if err != nil {
		return 0, err
	}
	return v.Int()
}

// CreateConfigureTaskbarCalendar creates ./User/Vendor/MSFT/Policy/Config/Settings/ConfigureTaskbarCalendar.
// Allows IT Admins to configure the default setting for showing additional calendars (besides the
// default calendar for the locale) in the taskbar clock and calendar flyout. In this version of
// Windows 10, supported additional calendars are: Simplified or Traditional Chinese lunar
// calendar. Turning on one of these calendars will display Chinese lunar dates below the default
// calendar for the locale. Select Don't show additional calendars to prevent showing other
// calendars besides the default calendar for the locale.
//
// Default: 0.
// Supported from OS build 10.0.15063 (CSP v5.0).
func (s *SettingsUser) CreateConfigureTaskbarCalendar(ctx context.Context, value int64) error {
	return s.c.Add(ctx, URIConfigureTaskbarCalendar, client.Int(value))
}

// UpdateConfigureTaskbarCalendar updates ./User/Vendor/MSFT/Policy/Config/Settings/ConfigureTaskbarCalendar.
// Allows IT Admins to configure the default setting for showing additional calendars (besides the
// default calendar for the locale) in the taskbar clock and calendar flyout. In this version of
// Windows 10, supported additional calendars are: Simplified or Traditional Chinese lunar
// calendar. Turning on one of these calendars will display Chinese lunar dates below the default
// calendar for the locale. Select Don't show additional calendars to prevent showing other
// calendars besides the default calendar for the locale.
//
// Default: 0.
// Supported from OS build 10.0.15063 (CSP v5.0).
func (s *SettingsUser) UpdateConfigureTaskbarCalendar(ctx context.Context, value int64) error {
	return s.c.Replace(ctx, URIConfigureTaskbarCalendar, client.Int(value))
}

// DeleteConfigureTaskbarCalendar deletes ./User/Vendor/MSFT/Policy/Config/Settings/ConfigureTaskbarCalendar.
// Allows IT Admins to configure the default setting for showing additional calendars (besides the
// default calendar for the locale) in the taskbar clock and calendar flyout. In this version of
// Windows 10, supported additional calendars are: Simplified or Traditional Chinese lunar
// calendar. Turning on one of these calendars will display Chinese lunar dates below the default
// calendar for the locale. Select Don't show additional calendars to prevent showing other
// calendars besides the default calendar for the locale.
//
// Default: 0.
// Supported from OS build 10.0.15063 (CSP v5.0).
func (s *SettingsUser) DeleteConfigureTaskbarCalendar(ctx context.Context) error {
	return s.c.Delete(ctx, URIConfigureTaskbarCalendar)
}

// GetPageVisibilityList reads ./User/Vendor/MSFT/Policy/Config/Settings/PageVisibilityList.
// Allows IT Admins to either prevent specific pages in the System Settings app from being visible
// or accessible, or to do so for all pages except those specified. The mode will be specified by
// the policy string beginning with either the string showonly: or hide:. Pages are identified by a
// shortened version of their already published URIs, which is the URI minus the ms-settings:
// prefix. For example, if the URI for a settings page is ms-settings:bluetooth, the page
// identifier used in the policy will be just bluetooth. Multiple page identifiers are separated by
// semicolons. The following example illustrates a policy that would allow access only to the about
// and bluetooth pages, which have URI ms-settings:about and ms-settings:bluetooth
// respectively:showonly:about;bluetooth. If the policy is not specified, the behavior will be that
// no pages are affected. If the policy string is formatted incorrectly, it will be ignored
// entirely (i. e. treated as not set) to prevent the machine from becoming unserviceable if data
// corruption occurs. Note that if a page is already hidden for another reason, then it will remain
// hidden even if it is in a showonly: list. The format of the PageVisibilityList value is as
// follows: The value is a unicode string up to 10,000 characters long, which will be used without
// case sensitivity. There are two variants: one that shows only the given pages and one which
// hides the given pages. The first variant starts with the string showonly: and the second with
// the string hide:. Following the variant identifier is a semicolon-delimited list of page
// identifiers, which must not have any extra whitespace. Each page identifier is the
// ms-settings:xyz URI for the page, minus the ms-settings: prefix, so the identifier for the page
// with URI ms-settings:network-wifi would be just network-wifi. The default value for this setting
// is an empty string, which is interpreted as show everything. Example 1, specifies that only the
// wifi and bluetooth pages should be shown (they have URIs ms-settings:network-wifi and
// ms-settings:bluetooth). All other pages (and the categories they're in) will be
// hidden:showonly:network-wifi;bluetooth. Example 2, specifies that the wifi page should not be
// shown:hide:network-wifi
//
// Supported from OS build 10.0.15063 (CSP v5.0).
func (s *SettingsUser) GetPageVisibilityList(ctx context.Context) (string, error) {
	v, err := s.c.Get(ctx, URIPageVisibilityList)
	if err != nil {
		return "", err
	}
	return v.Str(), nil
}

// CreatePageVisibilityList creates ./User/Vendor/MSFT/Policy/Config/Settings/PageVisibilityList.
// Allows IT Admins to either prevent specific pages in the System Settings app from being visible
// or accessible, or to do so for all pages except those specified. The mode will be specified by
// the policy string beginning with either the string showonly: or hide:. Pages are identified by a
// shortened version of their already published URIs, which is the URI minus the ms-settings:
// prefix. For example, if the URI for a settings page is ms-settings:bluetooth, the page
// identifier used in the policy will be just bluetooth. Multiple page identifiers are separated by
// semicolons. The following example illustrates a policy that would allow access only to the about
// and bluetooth pages, which have URI ms-settings:about and ms-settings:bluetooth
// respectively:showonly:about;bluetooth. If the policy is not specified, the behavior will be that
// no pages are affected. If the policy string is formatted incorrectly, it will be ignored
// entirely (i. e. treated as not set) to prevent the machine from becoming unserviceable if data
// corruption occurs. Note that if a page is already hidden for another reason, then it will remain
// hidden even if it is in a showonly: list. The format of the PageVisibilityList value is as
// follows: The value is a unicode string up to 10,000 characters long, which will be used without
// case sensitivity. There are two variants: one that shows only the given pages and one which
// hides the given pages. The first variant starts with the string showonly: and the second with
// the string hide:. Following the variant identifier is a semicolon-delimited list of page
// identifiers, which must not have any extra whitespace. Each page identifier is the
// ms-settings:xyz URI for the page, minus the ms-settings: prefix, so the identifier for the page
// with URI ms-settings:network-wifi would be just network-wifi. The default value for this setting
// is an empty string, which is interpreted as show everything. Example 1, specifies that only the
// wifi and bluetooth pages should be shown (they have URIs ms-settings:network-wifi and
// ms-settings:bluetooth). All other pages (and the categories they're in) will be
// hidden:showonly:network-wifi;bluetooth. Example 2, specifies that the wifi page should not be
// shown:hide:network-wifi
//
// Supported from OS build 10.0.15063 (CSP v5.0).
func (s *SettingsUser) CreatePageVisibilityList(ctx context.Context, value string) error {
	return s.c.Add(ctx, URIPageVisibilityList, client.Chr(value))
}

// UpdatePageVisibilityList updates ./User/Vendor/MSFT/Policy/Config/Settings/PageVisibilityList.
// Allows IT Admins to either prevent specific pages in the System Settings app from being visible
// or accessible, or to do so for all pages except those specified. The mode will be specified by
// the policy string beginning with either the string showonly: or hide:. Pages are identified by a
// shortened version of their already published URIs, which is the URI minus the ms-settings:
// prefix. For example, if the URI for a settings page is ms-settings:bluetooth, the page
// identifier used in the policy will be just bluetooth. Multiple page identifiers are separated by
// semicolons. The following example illustrates a policy that would allow access only to the about
// and bluetooth pages, which have URI ms-settings:about and ms-settings:bluetooth
// respectively:showonly:about;bluetooth. If the policy is not specified, the behavior will be that
// no pages are affected. If the policy string is formatted incorrectly, it will be ignored
// entirely (i. e. treated as not set) to prevent the machine from becoming unserviceable if data
// corruption occurs. Note that if a page is already hidden for another reason, then it will remain
// hidden even if it is in a showonly: list. The format of the PageVisibilityList value is as
// follows: The value is a unicode string up to 10,000 characters long, which will be used without
// case sensitivity. There are two variants: one that shows only the given pages and one which
// hides the given pages. The first variant starts with the string showonly: and the second with
// the string hide:. Following the variant identifier is a semicolon-delimited list of page
// identifiers, which must not have any extra whitespace. Each page identifier is the
// ms-settings:xyz URI for the page, minus the ms-settings: prefix, so the identifier for the page
// with URI ms-settings:network-wifi would be just network-wifi. The default value for this setting
// is an empty string, which is interpreted as show everything. Example 1, specifies that only the
// wifi and bluetooth pages should be shown (they have URIs ms-settings:network-wifi and
// ms-settings:bluetooth). All other pages (and the categories they're in) will be
// hidden:showonly:network-wifi;bluetooth. Example 2, specifies that the wifi page should not be
// shown:hide:network-wifi
//
// Supported from OS build 10.0.15063 (CSP v5.0).
func (s *SettingsUser) UpdatePageVisibilityList(ctx context.Context, value string) error {
	return s.c.Replace(ctx, URIPageVisibilityList, client.Chr(value))
}

// DeletePageVisibilityList deletes ./User/Vendor/MSFT/Policy/Config/Settings/PageVisibilityList.
// Allows IT Admins to either prevent specific pages in the System Settings app from being visible
// or accessible, or to do so for all pages except those specified. The mode will be specified by
// the policy string beginning with either the string showonly: or hide:. Pages are identified by a
// shortened version of their already published URIs, which is the URI minus the ms-settings:
// prefix. For example, if the URI for a settings page is ms-settings:bluetooth, the page
// identifier used in the policy will be just bluetooth. Multiple page identifiers are separated by
// semicolons. The following example illustrates a policy that would allow access only to the about
// and bluetooth pages, which have URI ms-settings:about and ms-settings:bluetooth
// respectively:showonly:about;bluetooth. If the policy is not specified, the behavior will be that
// no pages are affected. If the policy string is formatted incorrectly, it will be ignored
// entirely (i. e. treated as not set) to prevent the machine from becoming unserviceable if data
// corruption occurs. Note that if a page is already hidden for another reason, then it will remain
// hidden even if it is in a showonly: list. The format of the PageVisibilityList value is as
// follows: The value is a unicode string up to 10,000 characters long, which will be used without
// case sensitivity. There are two variants: one that shows only the given pages and one which
// hides the given pages. The first variant starts with the string showonly: and the second with
// the string hide:. Following the variant identifier is a semicolon-delimited list of page
// identifiers, which must not have any extra whitespace. Each page identifier is the
// ms-settings:xyz URI for the page, minus the ms-settings: prefix, so the identifier for the page
// with URI ms-settings:network-wifi would be just network-wifi. The default value for this setting
// is an empty string, which is interpreted as show everything. Example 1, specifies that only the
// wifi and bluetooth pages should be shown (they have URIs ms-settings:network-wifi and
// ms-settings:bluetooth). All other pages (and the categories they're in) will be
// hidden:showonly:network-wifi;bluetooth. Example 2, specifies that the wifi page should not be
// shown:hide:network-wifi
//
// Supported from OS build 10.0.15063 (CSP v5.0).
func (s *SettingsUser) DeletePageVisibilityList(ctx context.Context) error {
	return s.c.Delete(ctx, URIPageVisibilityList)
}
