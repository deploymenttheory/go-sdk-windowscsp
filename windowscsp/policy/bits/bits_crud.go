// Code generated by go-sdk-windowscsp-codegen. DO NOT EDIT.

package bits

import (
	"context"

	"github.com/deploymenttheory/go-sdk-windowscsp/windowscsp/client"
)

// GetBandwidthThrottlingEndTime reads ./Device/Vendor/MSFT/Policy/Config/BITS/BandwidthThrottlingEndTime.
// This policy specifies the bandwidth throttling end time that Background Intelligent Transfer
// Service (BITS) uses for background transfers. This policy setting does not affect foreground
// transfers. This policy is based on the 24-hour clock. Value type is integer. Default value is 17
// (5 PM). Supported value range: 0 - 23You can specify a limit to use during a specific time
// interval and at all other times. For example, limit the use of network bandwidth to 10 Kbps from
// 8:00 A. M. to 5:00 P. M. , and use all available unused bandwidth the rest of the day's hours.
// Using the three policies together (BandwidthThrottlingStartTime, BandwidthThrottlingEndTime,
// BandwidthThrottlingTransferRate), BITS will limit its bandwidth usage to the specified values.
// You can specify the limit in kilobits per second (Kbps). If you specify a value less than 2
// kilobits, BITS will continue to use approximately 2 kilobits. To prevent BITS transfers from
// occurring, specify a limit of 0. If you disable or do not configure this policy setting, BITS
// uses all available unused bandwidth. Note: You should base the limit on the speed of the network
// link, not the computer's network interface card (NIC). This policy setting does not affect peer
// caching transfers between peer computers (it does affect transfers from the origin server); the
// Limit the maximum network bandwidth used for Peercaching policy setting should be used for that
// purpose. Consider using this setting to prevent BITS transfers from competing for network
// bandwidth when the client computer has a fast network card (10Mbs), but is connected to the
// network via a slow link (56Kbs).
//
// Default: 17.
// Supported from OS build 10.0.17763 (CSP v8.0).
func (s *BITS) GetBandwidthThrottlingEndTime(ctx context.Context) (int64, error) {
	v, err := s.c.Get(ctx, URIBandwidthThrottlingEndTime)
	if err != nil {
		return 0, err
	}
	return v.Int()
}

// CreateBandwidthThrottlingEndTime creates ./Device/Vendor/MSFT/Policy/Config/BITS/BandwidthThrottlingEndTime.
// This policy specifies the bandwidth throttling end time that Background Intelligent Transfer
// Service (BITS) uses for background transfers. This policy setting does not affect foreground
// transfers. This policy is based on the 24-hour clock. Value type is integer. Default value is 17
// (5 PM). Supported value range: 0 - 23You can specify a limit to use during a specific time
// interval and at all other times. For example, limit the use of network bandwidth to 10 Kbps from
// 8:00 A. M. to 5:00 P. M. , and use all available unused bandwidth the rest of the day's hours.
// Using the three policies together (BandwidthThrottlingStartTime, BandwidthThrottlingEndTime,
// BandwidthThrottlingTransferRate), BITS will limit its bandwidth usage to the specified values.
// You can specify the limit in kilobits per second (Kbps). If you specify a value less than 2
// kilobits, BITS will continue to use approximately 2 kilobits. To prevent BITS transfers from
// occurring, specify a limit of 0. If you disable or do not configure this policy setting, BITS
// uses all available unused bandwidth. Note: You should base the limit on the speed of the network
// link, not the computer's network interface card (NIC). This policy setting does not affect peer
// caching transfers between peer computers (it does affect transfers from the origin server); the
// Limit the maximum network bandwidth used for Peercaching policy setting should be used for that
// purpose. Consider using this setting to prevent BITS transfers from competing for network
// bandwidth when the client computer has a fast network card (10Mbs), but is connected to the
// network via a slow link (56Kbs).
//
// Default: 17.
// Supported from OS build 10.0.17763 (CSP v8.0).
func (s *BITS) CreateBandwidthThrottlingEndTime(ctx context.Context, value int64) error {
	return s.c.Add(ctx, URIBandwidthThrottlingEndTime, client.Int(value))
}

// UpdateBandwidthThrottlingEndTime updates ./Device/Vendor/MSFT/Policy/Config/BITS/BandwidthThrottlingEndTime.
// This policy specifies the bandwidth throttling end time that Background Intelligent Transfer
// Service (BITS) uses for background transfers. This policy setting does not affect foreground
// transfers. This policy is based on the 24-hour clock. Value type is integer. Default value is 17
// (5 PM). Supported value range: 0 - 23You can specify a limit to use during a specific time
// interval and at all other times. For example, limit the use of network bandwidth to 10 Kbps from
// 8:00 A. M. to 5:00 P. M. , and use all available unused bandwidth the rest of the day's hours.
// Using the three policies together (BandwidthThrottlingStartTime, BandwidthThrottlingEndTime,
// BandwidthThrottlingTransferRate), BITS will limit its bandwidth usage to the specified values.
// You can specify the limit in kilobits per second (Kbps). If you specify a value less than 2
// kilobits, BITS will continue to use approximately 2 kilobits. To prevent BITS transfers from
// occurring, specify a limit of 0. If you disable or do not configure this policy setting, BITS
// uses all available unused bandwidth. Note: You should base the limit on the speed of the network
// link, not the computer's network interface card (NIC). This policy setting does not affect peer
// caching transfers between peer computers (it does affect transfers from the origin server); the
// Limit the maximum network bandwidth used for Peercaching policy setting should be used for that
// purpose. Consider using this setting to prevent BITS transfers from competing for network
// bandwidth when the client computer has a fast network card (10Mbs), but is connected to the
// network via a slow link (56Kbs).
//
// Default: 17.
// Supported from OS build 10.0.17763 (CSP v8.0).
func (s *BITS) UpdateBandwidthThrottlingEndTime(ctx context.Context, value int64) error {
	return s.c.Replace(ctx, URIBandwidthThrottlingEndTime, client.Int(value))
}

// DeleteBandwidthThrottlingEndTime deletes ./Device/Vendor/MSFT/Policy/Config/BITS/BandwidthThrottlingEndTime.
// This policy specifies the bandwidth throttling end time that Background Intelligent Transfer
// Service (BITS) uses for background transfers. This policy setting does not affect foreground
// transfers. This policy is based on the 24-hour clock. Value type is integer. Default value is 17
// (5 PM). Supported value range: 0 - 23You can specify a limit to use during a specific time
// interval and at all other times. For example, limit the use of network bandwidth to 10 Kbps from
// 8:00 A. M. to 5:00 P. M. , and use all available unused bandwidth the rest of the day's hours.
// Using the three policies together (BandwidthThrottlingStartTime, BandwidthThrottlingEndTime,
// BandwidthThrottlingTransferRate), BITS will limit its bandwidth usage to the specified values.
// You can specify the limit in kilobits per second (Kbps). If you specify a value less than 2
// kilobits, BITS will continue to use approximately 2 kilobits. To prevent BITS transfers from
// occurring, specify a limit of 0. If you disable or do not configure this policy setting, BITS
// uses all available unused bandwidth. Note: You should base the limit on the speed of the network
// link, not the computer's network interface card (NIC). This policy setting does not affect peer
// caching transfers between peer computers (it does affect transfers from the origin server); the
// Limit the maximum network bandwidth used for Peercaching policy setting should be used for that
// purpose. Consider using this setting to prevent BITS transfers from competing for network
// bandwidth when the client computer has a fast network card (10Mbs), but is connected to the
// network via a slow link (56Kbs).
//
// Default: 17.
// Supported from OS build 10.0.17763 (CSP v8.0).
func (s *BITS) DeleteBandwidthThrottlingEndTime(ctx context.Context) error {
	return s.c.Delete(ctx, URIBandwidthThrottlingEndTime)
}

// GetBandwidthThrottlingStartTime reads ./Device/Vendor/MSFT/Policy/Config/BITS/BandwidthThrottlingStartTime.
// This policy specifies the bandwidth throttling start time that Background Intelligent Transfer
// Service (BITS) uses for background transfers. This policy setting does not affect foreground
// transfers. This policy is based on the 24-hour clock. Value type is integer. Default value is 8
// (8 am). Supported value range: 0 - 23You can specify a limit to use during a specific time
// interval and at all other times. For example, limit the use of network bandwidth to 10 Kbps from
// 8:00 A. M. to 5:00 P. M. , and use all available unused bandwidth the rest of the day's hours.
// Using the three policies together (BandwidthThrottlingStartTime, BandwidthThrottlingEndTime,
// BandwidthThrottlingTransferRate), BITS will limit its bandwidth usage to the specified values.
// You can specify the limit in kilobits per second (Kbps). If you specify a value less than 2
// kilobits, BITS will continue to use approximately 2 kilobits. To prevent BITS transfers from
// occurring, specify a limit of 0. If you disable or do not configure this policy setting, BITS
// uses all available unused bandwidth. Note: You should base the limit on the speed of the network
// link, not the computer's network interface card (NIC). This policy setting does not affect peer
// caching transfers between peer computers (it does affect transfers from the origin server); the
// Limit the maximum network bandwidth used for Peercaching policy setting should be used for that
// purpose. Consider using this setting to prevent BITS transfers from competing for network
// bandwidth when the client computer has a fast network card (10Mbs), but is connected to the
// network via a slow link (56Kbs).
//
// Default: 8.
// Supported from OS build 10.0.17763 (CSP v8.0).
func (s *BITS) GetBandwidthThrottlingStartTime(ctx context.Context) (int64, error) {
	v, err := s.c.Get(ctx, URIBandwidthThrottlingStartTime)
	if err != nil {
		return 0, err
	}
	return v.Int()
}

// CreateBandwidthThrottlingStartTime creates ./Device/Vendor/MSFT/Policy/Config/BITS/BandwidthThrottlingStartTime.
// This policy specifies the bandwidth throttling start time that Background Intelligent Transfer
// Service (BITS) uses for background transfers. This policy setting does not affect foreground
// transfers. This policy is based on the 24-hour clock. Value type is integer. Default value is 8
// (8 am). Supported value range: 0 - 23You can specify a limit to use during a specific time
// interval and at all other times. For example, limit the use of network bandwidth to 10 Kbps from
// 8:00 A. M. to 5:00 P. M. , and use all available unused bandwidth the rest of the day's hours.
// Using the three policies together (BandwidthThrottlingStartTime, BandwidthThrottlingEndTime,
// BandwidthThrottlingTransferRate), BITS will limit its bandwidth usage to the specified values.
// You can specify the limit in kilobits per second (Kbps). If you specify a value less than 2
// kilobits, BITS will continue to use approximately 2 kilobits. To prevent BITS transfers from
// occurring, specify a limit of 0. If you disable or do not configure this policy setting, BITS
// uses all available unused bandwidth. Note: You should base the limit on the speed of the network
// link, not the computer's network interface card (NIC). This policy setting does not affect peer
// caching transfers between peer computers (it does affect transfers from the origin server); the
// Limit the maximum network bandwidth used for Peercaching policy setting should be used for that
// purpose. Consider using this setting to prevent BITS transfers from competing for network
// bandwidth when the client computer has a fast network card (10Mbs), but is connected to the
// network via a slow link (56Kbs).
//
// Default: 8.
// Supported from OS build 10.0.17763 (CSP v8.0).
func (s *BITS) CreateBandwidthThrottlingStartTime(ctx context.Context, value int64) error {
	return s.c.Add(ctx, URIBandwidthThrottlingStartTime, client.Int(value))
}

// UpdateBandwidthThrottlingStartTime updates ./Device/Vendor/MSFT/Policy/Config/BITS/BandwidthThrottlingStartTime.
// This policy specifies the bandwidth throttling start time that Background Intelligent Transfer
// Service (BITS) uses for background transfers. This policy setting does not affect foreground
// transfers. This policy is based on the 24-hour clock. Value type is integer. Default value is 8
// (8 am). Supported value range: 0 - 23You can specify a limit to use during a specific time
// interval and at all other times. For example, limit the use of network bandwidth to 10 Kbps from
// 8:00 A. M. to 5:00 P. M. , and use all available unused bandwidth the rest of the day's hours.
// Using the three policies together (BandwidthThrottlingStartTime, BandwidthThrottlingEndTime,
// BandwidthThrottlingTransferRate), BITS will limit its bandwidth usage to the specified values.
// You can specify the limit in kilobits per second (Kbps). If you specify a value less than 2
// kilobits, BITS will continue to use approximately 2 kilobits. To prevent BITS transfers from
// occurring, specify a limit of 0. If you disable or do not configure this policy setting, BITS
// uses all available unused bandwidth. Note: You should base the limit on the speed of the network
// link, not the computer's network interface card (NIC). This policy setting does not affect peer
// caching transfers between peer computers (it does affect transfers from the origin server); the
// Limit the maximum network bandwidth used for Peercaching policy setting should be used for that
// purpose. Consider using this setting to prevent BITS transfers from competing for network
// bandwidth when the client computer has a fast network card (10Mbs), but is connected to the
// network via a slow link (56Kbs).
//
// Default: 8.
// Supported from OS build 10.0.17763 (CSP v8.0).
func (s *BITS) UpdateBandwidthThrottlingStartTime(ctx context.Context, value int64) error {
	return s.c.Replace(ctx, URIBandwidthThrottlingStartTime, client.Int(value))
}

// DeleteBandwidthThrottlingStartTime deletes ./Device/Vendor/MSFT/Policy/Config/BITS/BandwidthThrottlingStartTime.
// This policy specifies the bandwidth throttling start time that Background Intelligent Transfer
// Service (BITS) uses for background transfers. This policy setting does not affect foreground
// transfers. This policy is based on the 24-hour clock. Value type is integer. Default value is 8
// (8 am). Supported value range: 0 - 23You can specify a limit to use during a specific time
// interval and at all other times. For example, limit the use of network bandwidth to 10 Kbps from
// 8:00 A. M. to 5:00 P. M. , and use all available unused bandwidth the rest of the day's hours.
// Using the three policies together (BandwidthThrottlingStartTime, BandwidthThrottlingEndTime,
// BandwidthThrottlingTransferRate), BITS will limit its bandwidth usage to the specified values.
// You can specify the limit in kilobits per second (Kbps). If you specify a value less than 2
// kilobits, BITS will continue to use approximately 2 kilobits. To prevent BITS transfers from
// occurring, specify a limit of 0. If you disable or do not configure this policy setting, BITS
// uses all available unused bandwidth. Note: You should base the limit on the speed of the network
// link, not the computer's network interface card (NIC). This policy setting does not affect peer
// caching transfers between peer computers (it does affect transfers from the origin server); the
// Limit the maximum network bandwidth used for Peercaching policy setting should be used for that
// purpose. Consider using this setting to prevent BITS transfers from competing for network
// bandwidth when the client computer has a fast network card (10Mbs), but is connected to the
// network via a slow link (56Kbs).
//
// Default: 8.
// Supported from OS build 10.0.17763 (CSP v8.0).
func (s *BITS) DeleteBandwidthThrottlingStartTime(ctx context.Context) error {
	return s.c.Delete(ctx, URIBandwidthThrottlingStartTime)
}

// GetBandwidthThrottlingTransferRate reads ./Device/Vendor/MSFT/Policy/Config/BITS/BandwidthThrottlingTransferRate.
// This policy specifies the bandwidth throttling transfer rate in kilobits per second (Kbps) that
// Background Intelligent Transfer Service (BITS) uses for background transfers. This policy
// setting does not affect foreground transfers. Value type is integer. Default value is 1000.
// Supported value range: 0 - 4294967200. You can specify a limit to use during a specific time
// interval and at all other times. For example, limit the use of network bandwidth to 10 Kbps from
// 8:00 A. M. to 5:00 P. M. , and use all available unused bandwidth the rest of the day's hours.
// Using the three policies together (BandwidthThrottlingStartTime, BandwidthThrottlingEndTime,
// BandwidthThrottlingTransferRate), BITS will limit its bandwidth usage to the specified values.
// You can specify the limit in kilobits per second (Kbps). If you specify a value less than 2
// kilobits, BITS will continue to use approximately 2 kilobits. To prevent BITS transfers from
// occurring, specify a limit of 0. If you disable or do not configure this policy setting, BITS
// uses all available unused bandwidth. Note: You should base the limit on the speed of the network
// link, not the computer's network interface card (NIC). This policy setting does not affect peer
// caching transfers between peer computers (it does affect transfers from the origin server); the
// Limit the maximum network bandwidth used for Peercaching policy setting should be used for that
// purpose. Consider using this setting to prevent BITS transfers from competing for network
// bandwidth when the client computer has a fast network card (10Mbs), but is connected to the
// network via a slow link (56Kbs).
//
// Default: 1000.
// Supported from OS build 10.0.17763 (CSP v8.0).
func (s *BITS) GetBandwidthThrottlingTransferRate(ctx context.Context) (int64, error) {
	v, err := s.c.Get(ctx, URIBandwidthThrottlingTransferRate)
	if err != nil {
		return 0, err
	}
	return v.Int()
}

// CreateBandwidthThrottlingTransferRate creates ./Device/Vendor/MSFT/Policy/Config/BITS/BandwidthThrottlingTransferRate.
// This policy specifies the bandwidth throttling transfer rate in kilobits per second (Kbps) that
// Background Intelligent Transfer Service (BITS) uses for background transfers. This policy
// setting does not affect foreground transfers. Value type is integer. Default value is 1000.
// Supported value range: 0 - 4294967200. You can specify a limit to use during a specific time
// interval and at all other times. For example, limit the use of network bandwidth to 10 Kbps from
// 8:00 A. M. to 5:00 P. M. , and use all available unused bandwidth the rest of the day's hours.
// Using the three policies together (BandwidthThrottlingStartTime, BandwidthThrottlingEndTime,
// BandwidthThrottlingTransferRate), BITS will limit its bandwidth usage to the specified values.
// You can specify the limit in kilobits per second (Kbps). If you specify a value less than 2
// kilobits, BITS will continue to use approximately 2 kilobits. To prevent BITS transfers from
// occurring, specify a limit of 0. If you disable or do not configure this policy setting, BITS
// uses all available unused bandwidth. Note: You should base the limit on the speed of the network
// link, not the computer's network interface card (NIC). This policy setting does not affect peer
// caching transfers between peer computers (it does affect transfers from the origin server); the
// Limit the maximum network bandwidth used for Peercaching policy setting should be used for that
// purpose. Consider using this setting to prevent BITS transfers from competing for network
// bandwidth when the client computer has a fast network card (10Mbs), but is connected to the
// network via a slow link (56Kbs).
//
// Default: 1000.
// Supported from OS build 10.0.17763 (CSP v8.0).
func (s *BITS) CreateBandwidthThrottlingTransferRate(ctx context.Context, value int64) error {
	return s.c.Add(ctx, URIBandwidthThrottlingTransferRate, client.Int(value))
}

// UpdateBandwidthThrottlingTransferRate updates ./Device/Vendor/MSFT/Policy/Config/BITS/BandwidthThrottlingTransferRate.
// This policy specifies the bandwidth throttling transfer rate in kilobits per second (Kbps) that
// Background Intelligent Transfer Service (BITS) uses for background transfers. This policy
// setting does not affect foreground transfers. Value type is integer. Default value is 1000.
// Supported value range: 0 - 4294967200. You can specify a limit to use during a specific time
// interval and at all other times. For example, limit the use of network bandwidth to 10 Kbps from
// 8:00 A. M. to 5:00 P. M. , and use all available unused bandwidth the rest of the day's hours.
// Using the three policies together (BandwidthThrottlingStartTime, BandwidthThrottlingEndTime,
// BandwidthThrottlingTransferRate), BITS will limit its bandwidth usage to the specified values.
// You can specify the limit in kilobits per second (Kbps). If you specify a value less than 2
// kilobits, BITS will continue to use approximately 2 kilobits. To prevent BITS transfers from
// occurring, specify a limit of 0. If you disable or do not configure this policy setting, BITS
// uses all available unused bandwidth. Note: You should base the limit on the speed of the network
// link, not the computer's network interface card (NIC). This policy setting does not affect peer
// caching transfers between peer computers (it does affect transfers from the origin server); the
// Limit the maximum network bandwidth used for Peercaching policy setting should be used for that
// purpose. Consider using this setting to prevent BITS transfers from competing for network
// bandwidth when the client computer has a fast network card (10Mbs), but is connected to the
// network via a slow link (56Kbs).
//
// Default: 1000.
// Supported from OS build 10.0.17763 (CSP v8.0).
func (s *BITS) UpdateBandwidthThrottlingTransferRate(ctx context.Context, value int64) error {
	return s.c.Replace(ctx, URIBandwidthThrottlingTransferRate, client.Int(value))
}

// DeleteBandwidthThrottlingTransferRate deletes ./Device/Vendor/MSFT/Policy/Config/BITS/BandwidthThrottlingTransferRate.
// This policy specifies the bandwidth throttling transfer rate in kilobits per second (Kbps) that
// Background Intelligent Transfer Service (BITS) uses for background transfers. This policy
// setting does not affect foreground transfers. Value type is integer. Default value is 1000.
// Supported value range: 0 - 4294967200. You can specify a limit to use during a specific time
// interval and at all other times. For example, limit the use of network bandwidth to 10 Kbps from
// 8:00 A. M. to 5:00 P. M. , and use all available unused bandwidth the rest of the day's hours.
// Using the three policies together (BandwidthThrottlingStartTime, BandwidthThrottlingEndTime,
// BandwidthThrottlingTransferRate), BITS will limit its bandwidth usage to the specified values.
// You can specify the limit in kilobits per second (Kbps). If you specify a value less than 2
// kilobits, BITS will continue to use approximately 2 kilobits. To prevent BITS transfers from
// occurring, specify a limit of 0. If you disable or do not configure this policy setting, BITS
// uses all available unused bandwidth. Note: You should base the limit on the speed of the network
// link, not the computer's network interface card (NIC). This policy setting does not affect peer
// caching transfers between peer computers (it does affect transfers from the origin server); the
// Limit the maximum network bandwidth used for Peercaching policy setting should be used for that
// purpose. Consider using this setting to prevent BITS transfers from competing for network
// bandwidth when the client computer has a fast network card (10Mbs), but is connected to the
// network via a slow link (56Kbs).
//
// Default: 1000.
// Supported from OS build 10.0.17763 (CSP v8.0).
func (s *BITS) DeleteBandwidthThrottlingTransferRate(ctx context.Context) error {
	return s.c.Delete(ctx, URIBandwidthThrottlingTransferRate)
}

// GetCostedNetworkBehaviorBackgroundPriority reads ./Device/Vendor/MSFT/Policy/Config/BITS/CostedNetworkBehaviorBackgroundPriority.
// This policy setting defines the default behavior that the Background Intelligent Transfer
// Service (BITS) uses for background transfers when the system is connected to a costed network
// (3G, etc. ). Download behavior policies further limit the network usage of background transfers.
// If you enable this policy setting, you can define a default download policy for each BITS job
// priority. This setting does not override a download policy explicitly configured by the
// application that created the BITS job, but does apply to jobs that are created by specifying
// only a priority. For example, you can specify that background jobs are by default to transfer
// only when on uncosted network connections, but foreground jobs should proceed only when not
// roaming. The values that can be assigned are:1 - Always transfer2 - Transfer unless roaming3 -
// Transfer unless surcharge applies (when not roaming or overcap)4 - Transfer unless nearing limit
// (when not roaming or nearing cap)5 - Transfer only if unconstrained
//
// Default: 1.
// Supported from OS build 10.0.17763 (CSP v8.0).
func (s *BITS) GetCostedNetworkBehaviorBackgroundPriority(ctx context.Context) (int64, error) {
	v, err := s.c.Get(ctx, URICostedNetworkBehaviorBackgroundPriority)
	if err != nil {
		return 0, err
	}
	return v.Int()
}

// CreateCostedNetworkBehaviorBackgroundPriority creates ./Device/Vendor/MSFT/Policy/Config/BITS/CostedNetworkBehaviorBackgroundPriority.
// This policy setting defines the default behavior that the Background Intelligent Transfer
// Service (BITS) uses for background transfers when the system is connected to a costed network
// (3G, etc. ). Download behavior policies further limit the network usage of background transfers.
// If you enable this policy setting, you can define a default download policy for each BITS job
// priority. This setting does not override a download policy explicitly configured by the
// application that created the BITS job, but does apply to jobs that are created by specifying
// only a priority. For example, you can specify that background jobs are by default to transfer
// only when on uncosted network connections, but foreground jobs should proceed only when not
// roaming. The values that can be assigned are:1 - Always transfer2 - Transfer unless roaming3 -
// Transfer unless surcharge applies (when not roaming or overcap)4 - Transfer unless nearing limit
// (when not roaming or nearing cap)5 - Transfer only if unconstrained
//
// Default: 1.
// Supported from OS build 10.0.17763 (CSP v8.0).
func (s *BITS) CreateCostedNetworkBehaviorBackgroundPriority(ctx context.Context, value int64) error {
	return s.c.Add(ctx, URICostedNetworkBehaviorBackgroundPriority, client.Int(value))
}

// UpdateCostedNetworkBehaviorBackgroundPriority updates ./Device/Vendor/MSFT/Policy/Config/BITS/CostedNetworkBehaviorBackgroundPriority.
// This policy setting defines the default behavior that the Background Intelligent Transfer
// Service (BITS) uses for background transfers when the system is connected to a costed network
// (3G, etc. ). Download behavior policies further limit the network usage of background transfers.
// If you enable this policy setting, you can define a default download policy for each BITS job
// priority. This setting does not override a download policy explicitly configured by the
// application that created the BITS job, but does apply to jobs that are created by specifying
// only a priority. For example, you can specify that background jobs are by default to transfer
// only when on uncosted network connections, but foreground jobs should proceed only when not
// roaming. The values that can be assigned are:1 - Always transfer2 - Transfer unless roaming3 -
// Transfer unless surcharge applies (when not roaming or overcap)4 - Transfer unless nearing limit
// (when not roaming or nearing cap)5 - Transfer only if unconstrained
//
// Default: 1.
// Supported from OS build 10.0.17763 (CSP v8.0).
func (s *BITS) UpdateCostedNetworkBehaviorBackgroundPriority(ctx context.Context, value int64) error {
	return s.c.Replace(ctx, URICostedNetworkBehaviorBackgroundPriority, client.Int(value))
}

// DeleteCostedNetworkBehaviorBackgroundPriority deletes ./Device/Vendor/MSFT/Policy/Config/BITS/CostedNetworkBehaviorBackgroundPriority.
// This policy setting defines the default behavior that the Background Intelligent Transfer
// Service (BITS) uses for background transfers when the system is connected to a costed network
// (3G, etc. ). Download behavior policies further limit the network usage of background transfers.
// If you enable this policy setting, you can define a default download policy for each BITS job
// priority. This setting does not override a download policy explicitly configured by the
// application that created the BITS job, but does apply to jobs that are created by specifying
// only a priority. For example, you can specify that background jobs are by default to transfer
// only when on uncosted network connections, but foreground jobs should proceed only when not
// roaming. The values that can be assigned are:1 - Always transfer2 - Transfer unless roaming3 -
// Transfer unless surcharge applies (when not roaming or overcap)4 - Transfer unless nearing limit
// (when not roaming or nearing cap)5 - Transfer only if unconstrained
//
// Default: 1.
// Supported from OS build 10.0.17763 (CSP v8.0).
func (s *BITS) DeleteCostedNetworkBehaviorBackgroundPriority(ctx context.Context) error {
	return s.c.Delete(ctx, URICostedNetworkBehaviorBackgroundPriority)
}

// GetCostedNetworkBehaviorForegroundPriority reads ./Device/Vendor/MSFT/Policy/Config/BITS/CostedNetworkBehaviorForegroundPriority.
// This policy setting defines the default behavior that the foreground Intelligent Transfer
// Service (BITS) uses for foreground transfers when the system is connected to a costed network
// (3G, etc. ). Download behavior policies further limit the network usage of foreground transfers.
// If you enable this policy setting, you can define a default download policy for each BITS job
// priority. This setting does not override a download policy explicitly configured by the
// application that created the BITS job, but does apply to jobs that are created by specifying
// only a priority. For example, you can specify that foreground jobs are by default to transfer
// only when on uncosted network connections, but foreground jobs should proceed only when not
// roaming. The values that can be assigned are:1 - Always transfer2 - Transfer unless roaming3 -
// Transfer unless surcharge applies (when not roaming or overcap)4 - Transfer unless nearing limit
// (when not roaming or nearing cap)5 - Transfer only if unconstrained
//
// Default: 1.
// Supported from OS build 10.0.17763 (CSP v8.0).
func (s *BITS) GetCostedNetworkBehaviorForegroundPriority(ctx context.Context) (int64, error) {
	v, err := s.c.Get(ctx, URICostedNetworkBehaviorForegroundPriority)
	if err != nil {
		return 0, err
	}
	return v.Int()
}

// CreateCostedNetworkBehaviorForegroundPriority creates ./Device/Vendor/MSFT/Policy/Config/BITS/CostedNetworkBehaviorForegroundPriority.
// This policy setting defines the default behavior that the foreground Intelligent Transfer
// Service (BITS) uses for foreground transfers when the system is connected to a costed network
// (3G, etc. ). Download behavior policies further limit the network usage of foreground transfers.
// If you enable this policy setting, you can define a default download policy for each BITS job
// priority. This setting does not override a download policy explicitly configured by the
// application that created the BITS job, but does apply to jobs that are created by specifying
// only a priority. For example, you can specify that foreground jobs are by default to transfer
// only when on uncosted network connections, but foreground jobs should proceed only when not
// roaming. The values that can be assigned are:1 - Always transfer2 - Transfer unless roaming3 -
// Transfer unless surcharge applies (when not roaming or overcap)4 - Transfer unless nearing limit
// (when not roaming or nearing cap)5 - Transfer only if unconstrained
//
// Default: 1.
// Supported from OS build 10.0.17763 (CSP v8.0).
func (s *BITS) CreateCostedNetworkBehaviorForegroundPriority(ctx context.Context, value int64) error {
	return s.c.Add(ctx, URICostedNetworkBehaviorForegroundPriority, client.Int(value))
}

// UpdateCostedNetworkBehaviorForegroundPriority updates ./Device/Vendor/MSFT/Policy/Config/BITS/CostedNetworkBehaviorForegroundPriority.
// This policy setting defines the default behavior that the foreground Intelligent Transfer
// Service (BITS) uses for foreground transfers when the system is connected to a costed network
// (3G, etc. ). Download behavior policies further limit the network usage of foreground transfers.
// If you enable this policy setting, you can define a default download policy for each BITS job
// priority. This setting does not override a download policy explicitly configured by the
// application that created the BITS job, but does apply to jobs that are created by specifying
// only a priority. For example, you can specify that foreground jobs are by default to transfer
// only when on uncosted network connections, but foreground jobs should proceed only when not
// roaming. The values that can be assigned are:1 - Always transfer2 - Transfer unless roaming3 -
// Transfer unless surcharge applies (when not roaming or overcap)4 - Transfer unless nearing limit
// (when not roaming or nearing cap)5 - Transfer only if unconstrained
//
// Default: 1.
// Supported from OS build 10.0.17763 (CSP v8.0).
func (s *BITS) UpdateCostedNetworkBehaviorForegroundPriority(ctx context.Context, value int64) error {
	return s.c.Replace(ctx, URICostedNetworkBehaviorForegroundPriority, client.Int(value))
}

// DeleteCostedNetworkBehaviorForegroundPriority deletes ./Device/Vendor/MSFT/Policy/Config/BITS/CostedNetworkBehaviorForegroundPriority.
// This policy setting defines the default behavior that the foreground Intelligent Transfer
// Service (BITS) uses for foreground transfers when the system is connected to a costed network
// (3G, etc. ). Download behavior policies further limit the network usage of foreground transfers.
// If you enable this policy setting, you can define a default download policy for each BITS job
// priority. This setting does not override a download policy explicitly configured by the
// application that created the BITS job, but does apply to jobs that are created by specifying
// only a priority. For example, you can specify that foreground jobs are by default to transfer
// only when on uncosted network connections, but foreground jobs should proceed only when not
// roaming. The values that can be assigned are:1 - Always transfer2 - Transfer unless roaming3 -
// Transfer unless surcharge applies (when not roaming or overcap)4 - Transfer unless nearing limit
// (when not roaming or nearing cap)5 - Transfer only if unconstrained
//
// Default: 1.
// Supported from OS build 10.0.17763 (CSP v8.0).
func (s *BITS) DeleteCostedNetworkBehaviorForegroundPriority(ctx context.Context) error {
	return s.c.Delete(ctx, URICostedNetworkBehaviorForegroundPriority)
}

// GetJobInactivityTimeout reads ./Device/Vendor/MSFT/Policy/Config/BITS/JobInactivityTimeout.
// This policy setting specifies the number of days a pending BITS job can remain inactive before
// the job is considered abandoned. By default BITS will wait 90 days before considering an
// inactive job abandoned. After a job is determined to be abandoned, the job is deleted from BITS
// and any downloaded files for the job are deleted from the disk. NoteAny property changes to the
// job or any successful download action will reset this timeout. Value type is integer. Default is
// 90 days. Supported values range: 0 - 999Consider increasing the timeout value if computers tend
// to stay offline for a long period of time and still have pending jobs. Consider decreasing this
// value if you are concerned about orphaned jobs occupying disk space. If you disable or do not
// configure this policy setting, the default value of 90 (days) will be used for the inactive job
// timeout.
//
// Default: 90.
// Supported from OS build 10.0.17763 (CSP v8.0).
func (s *BITS) GetJobInactivityTimeout(ctx context.Context) (int64, error) {
	v, err := s.c.Get(ctx, URIJobInactivityTimeout)
	if err != nil {
		return 0, err
	}
	return v.Int()
}

// CreateJobInactivityTimeout creates ./Device/Vendor/MSFT/Policy/Config/BITS/JobInactivityTimeout.
// This policy setting specifies the number of days a pending BITS job can remain inactive before
// the job is considered abandoned. By default BITS will wait 90 days before considering an
// inactive job abandoned. After a job is determined to be abandoned, the job is deleted from BITS
// and any downloaded files for the job are deleted from the disk. NoteAny property changes to the
// job or any successful download action will reset this timeout. Value type is integer. Default is
// 90 days. Supported values range: 0 - 999Consider increasing the timeout value if computers tend
// to stay offline for a long period of time and still have pending jobs. Consider decreasing this
// value if you are concerned about orphaned jobs occupying disk space. If you disable or do not
// configure this policy setting, the default value of 90 (days) will be used for the inactive job
// timeout.
//
// Default: 90.
// Supported from OS build 10.0.17763 (CSP v8.0).
func (s *BITS) CreateJobInactivityTimeout(ctx context.Context, value int64) error {
	return s.c.Add(ctx, URIJobInactivityTimeout, client.Int(value))
}

// UpdateJobInactivityTimeout updates ./Device/Vendor/MSFT/Policy/Config/BITS/JobInactivityTimeout.
// This policy setting specifies the number of days a pending BITS job can remain inactive before
// the job is considered abandoned. By default BITS will wait 90 days before considering an
// inactive job abandoned. After a job is determined to be abandoned, the job is deleted from BITS
// and any downloaded files for the job are deleted from the disk. NoteAny property changes to the
// job or any successful download action will reset this timeout. Value type is integer. Default is
// 90 days. Supported values range: 0 - 999Consider increasing the timeout value if computers tend
// to stay offline for a long period of time and still have pending jobs. Consider decreasing this
// value if you are concerned about orphaned jobs occupying disk space. If you disable or do not
// configure this policy setting, the default value of 90 (days) will be used for the inactive job
// timeout.
//
// Default: 90.
// Supported from OS build 10.0.17763 (CSP v8.0).
func (s *BITS) UpdateJobInactivityTimeout(ctx context.Context, value int64) error {
	return s.c.Replace(ctx, URIJobInactivityTimeout, client.Int(value))
}

// DeleteJobInactivityTimeout deletes ./Device/Vendor/MSFT/Policy/Config/BITS/JobInactivityTimeout.
// This policy setting specifies the number of days a pending BITS job can remain inactive before
// the job is considered abandoned. By default BITS will wait 90 days before considering an
// inactive job abandoned. After a job is determined to be abandoned, the job is deleted from BITS
// and any downloaded files for the job are deleted from the disk. NoteAny property changes to the
// job or any successful download action will reset this timeout. Value type is integer. Default is
// 90 days. Supported values range: 0 - 999Consider increasing the timeout value if computers tend
// to stay offline for a long period of time and still have pending jobs. Consider decreasing this
// value if you are concerned about orphaned jobs occupying disk space. If you disable or do not
// configure this policy setting, the default value of 90 (days) will be used for the inactive job
// timeout.
//
// Default: 90.
// Supported from OS build 10.0.17763 (CSP v8.0).
func (s *BITS) DeleteJobInactivityTimeout(ctx context.Context) error {
	return s.c.Delete(ctx, URIJobInactivityTimeout)
}
